# 选品选股器核心架构 — 策略与数据源解耦方案

> **项目**: CBHunter — 多平台跨境电商卖家后台  
> **文档类型**: 核心架构设计  
> **版本**: v2.0  
> **日期**: 2026-07-21  
> **对标参考**: 股票选股器（同花顺/东方财富/TradingView）  
> **核心原则**: 策略先行、数据后置、策略与数据源彻底解耦

---

## 一、架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                        CBHunter 选品选股器                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │   策略配置层      │  │   运算引擎层      │  │   结果展示层      │  │
│  │  Strategy Config  │→│   Engine Core    │→│  Result Display  │  │
│  │                  │  │                  │  │                  │  │
│  │ • 原生策略库      │  │ • 交集/并集/差集 │  │ • 标杆样例商品    │  │
│  │ • 复合策略库      │  │ • 加权评分排序    │  │ • 类目分布       │  │
│  │ • 参数阈值配置    │  │ • 去重/合并       │  │ • 趋势图表       │  │
│  │ • 策略嵌套树      │  │ • 数据缺失标记    │  │ • 联动操作入口    │  │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘  │
│           │                     │                     │             │
│           └─────────────┬───────┴─────────────────────┘             │
│                         ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                    数据源映射层                               │    │
│  │              Data Source Mapping Layer                       │    │
│  │                                                             │    │
│  │  策略ID → 数据源ID → 字段映射 → 采集方式 → 刷新频率            │    │
│  │                                                             │    │
│  │  [A级爬虫]  [B级API]  [C级导入]  [D级预留]                   │    │
│  └──────────────────────┬──────────────────────────────────────┘    │
│                         ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                    数据采集层                                 │    │
│  │                Collection Layer                              │    │
│  │                                                             │    │
│  │  TikTok公域  Shopee公域  Temu公域  Amazon公域                 │    │
│  │  Lazada公域  AliExpress公域  第三方工具API                      │    │
│  │  妙手ERP同步  卖家授权API  Excel导入  手动补录                  │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**核心设计思想**：
- **策略层不感知数据层**：策略只声明"我需要什么数据"，不关心数据从哪来
- **数据层不感知策略层**：数据源只提供原始数据，不关心谁在用
- **映射层做桥梁**：策略ID ↔ 数据源ID 的映射关系可独立维护
- **无数据不影响使用**：D级策略可正常配置、组合、保存，仅运算时标记"数据缺失"

---

## 二、四层架构详细设计

### 2.1 策略配置层（Strategy Configuration Layer）

**职责**：管理所有选品策略的定义、参数、阈值、组合关系。

**核心实体**：

```
原生策略（BaseStrategy）
├── strategy_id: 唯一标识（如 BS-01）
├── strategy_name: 策略名称
├── category: 所属大类（爆款/蓝海/利润/竞争/趋势/合规）
├── data_feasibility: 数据可行性等级（A/B/C/D）
├── platforms: 适配平台列表
├── parameters: 可参数化配置项（JSON Schema）
├── thresholds: 阈值调节项（JSON Schema）
├── data_source_ref: 关联的数据源引用（可为空，D级策略为空）
├── status: 启用/停用
└── created_at / updated_at

复合策略（CompositeStrategy）
├── composite_id: 唯一标识
├── name: 策略名称
├── operator: 运算类型（intersection/union/difference/weighted）
├── children: 子节点列表（可递归嵌套）
│   ├── type: base 或 composite
│   ├── ref_id: 引用原生策略或复合策略ID
│   └── weight: 权重（0-1，加权模式用）
├── parent_id: 父复合策略ID（多级嵌套用）
├── saved_as_new: 是否另存为新策略
└── version: 策略版本号（支持迭代）
```

**嵌套结构示例**：

```json
{
  "composite_id": "CS-001",
  "name": "TikTok菲律宾蓝海爆款",
  "operator": "intersection",
  "children": [
    {
      "type": "base",
      "ref_id": "BS-01",
      "weight": 0.3,
      "config": {"top_n": 50, "time_window": "7d"}
    },
    {
      "type": "base",
      "ref_id": "BL-01",
      "weight": 0.3,
      "config": {"min_search_volume": 1000, "max_product_count": 100}
    },
    {
      "type": "composite",
      "ref_id": "CS-002",
      "weight": 0.4,
      "config": {}
    }
  ]
}
```

### 2.2 运算引擎层（Engine Core）

**职责**：接收策略配置，执行交/并/差/加权运算，输出候选商品池。

**运算流程**：

```
输入：复合策略树 + 各叶子节点参数配置
  ↓
步骤1：遍历策略树，收集所有叶子节点（原生策略）
  ↓
步骤2：按叶子节点data_source_ref查询可用数据源
  ↓
步骤3：对每个叶子节点执行数据采集（A/B/C级）或标记缺失（D级）
  ↓
步骤4：从底层向上逐层执行运算
  ├─ 叶子节点：返回商品集合（或空集合+缺失标记）
  ├─ 交集节点：取各子节点结果的交集
  ├─ 并集节点：取各子节点结果的并集
  ├─ 差集节点：从父节点中排除子节点
  └─ 加权节点：对商品按各策略得分加权求和
  ↓
步骤5：汇总运算结果，标记数据缺失情况
  ↓
输出：候选商品池 + 数据完整性报告
```

**运算引擎接口**：

```typescript
interface SelectionEngine {
  /**
   * 执行复合策略运算
   * @param compositeStrategy 复合策略树
   * @param userConfig 用户参数配置覆盖
   * @returns 运算结果
   */
  execute(compositeStrategy: CompositeStrategy, userConfig?: Record<string, any>): Promise<SelectionResult>;

  /**
   * 验证策略树合法性
   */
  validate(strategyTree: CompositeStrategy): ValidationResult;

  /**
   * 模拟运算（不实际采集数据，用于UI预览）
   */
  simulate(strategyTree: CompositeStrategy): SimulationResult;

  /**
   * 历史回测（用历史数据验证策略有效性）
   */
  backtest(strategyTree: CompositeStrategy, period: DateRange): BacktestResult;
}

interface SelectionResult {
  products: CandidateProduct[];        // 候选商品列表
  totalMatched: number;                // 匹配总数
  dataMissing: DataMissingReport[];    // 数据缺失报告
  executionTime: number;               // 执行耗时(ms)
  strategyTrace: OperationTrace[];     // 每层运算追踪
}

interface DataMissingReport {
  strategyId: string;
  strategyName: string;
  dataFeasibility: 'A' | 'B' | 'C' | 'D';
  message: string;                     // "该策略当前无数据源，已跳过"
}
```

### 2.3 数据源映射层（Data Source Mapping Layer）

**职责**：建立策略ID到数据源的映射关系，支持独立升级数据源而不影响策略。

**核心设计**：

```
数据源注册表（DataSourceRegistry）
├── 数据源ID
├── 数据源名称
├── 数据源类型（crawler/api/import/manual）
├── 适配平台列表
├── 采集URL/接口配置
├── 字段映射规则（原始字段 → 标准字段）
├── 刷新频率
├── 健康状态
└── 元数据（认证信息、限流规则等）

策略-数据源映射表（StrategyDataSourceMapping）
├── 策略ID
├── 数据源ID（可为空，D级策略）
├── 映射字段（策略参数 → 数据源字段）
├── 优先级（同一策略可映射多个数据源时的优先级）
└── 状态（active/paused/deprecated）
```

**映射关系示例**：

| 策略ID | 策略名称 | 数据源ID | 数据源类型 | 数据级别 | 状态 |
|--------|----------|----------|-----------|----------|------|
| BS-01 | 平台榜单Top N | DS-TIKTOK-BESTSELLER | crawler | A | active |
| BS-01 | 平台榜单Top N | DS-SHOPEE-BESTSELLER | crawler | A | active |
| PF-01 | 毛利率筛选 | DS-COST-IMPORT | import | C | active |
| TR-07 | 行业新闻追踪 | null | none | D | reserved |
| RC-03 | 认证要求检查 | DS-SHOPEE-RULES | api | B | active |

**关键特性**：
- 同一策略可映射多个数据源（如BS-01映射TikTok/Shopee/Temu各自榜单）
- 数据源下线时，策略不受影响，仅标记"数据不可用"
- 新增数据源时，只需注册新数据源并建立映射，**无需修改策略定义**

### 2.4 数据采集层（Collection Layer）

**职责**：根据数据源类型执行实际数据采集。

**采集方式矩阵**：

| 数据级别 | 采集方式 | 实现技术 | 适用场景 | 限制 |
|----------|----------|----------|----------|------|
| A级 | 公域爬虫 | Chrome DevTools MCP / Playwright只读 | 榜单、搜索量、评论数 | 受反爬限制，需控制频率 |
| B级 | 官方API | 平台开放平台API / 妙手ERP授权 | 销量、订单、广告数据 | 需店铺授权，有调用限额 |
| C级 | 人工导入 | Excel/CSV上传 + 表单填写 | 成本价、关税、行业数据 | 依赖用户主动性 |
| D级 | 预留 | 暂无 | 纯策略配置项 | 等待数据源开发 |

---

## 三、策略与数据源解耦的关键机制

### 3.1 数据源热替换

当某个数据源失效或升级时：

```
旧数据源 DS-A（爬虫，A级）
  → 映射到策略 BS-01
  → 数据源失效

新数据源 DS-B（API，B级）
  → 注册新数据源
  → 更新映射：BS-01 → DS-B（优先级更高）
  → 旧映射标记为 deprecated

策略 BS-01 本身无需任何修改
```

### 3.2 D级策略生命周期

```
创建D级策略（无数据源）
  → 正常入库、显示、配置、组合
  → 运算时标记"数据缺失"
  ↓
用户手动导入数据（升级为C级）
  → 更新数据源映射
  → 策略自动生效
  ↓
开发爬虫/API（升级为A/B级）
  → 注册新数据源
  → 更新映射优先级
  → 策略自动升级为自动化执行
```

### 3.3 策略版本管理

复合策略支持版本迭代：

```
CS-001 v1.0（初始版本）
  → 用户调整参数后保存 v1.1
  → 添加新的子策略节点保存 v2.0
  → 每次变更生成新版本
  → 旧版本可回溯、对比、恢复
```

---

## 四、与现有CBHunter系统融合点

### 4.1 打通素材图库

选品结果中的标杆商品图片/视频可：
- 一键收藏到CBHunter素材图库
- 作为后续商品发布时的参考素材
- 支持批量下载、分类管理

### 4.2 打通商品发布模块

选品结果可：
- 直接转化为待发布商品草稿
- 复用01_多平台电商后台调研汇总中的字段映射
- 通过02_功能开发方案总文档中的弹窗式编辑流程上架

### 4.3 打通SKU定价模块

利润导向策略（PF系列）的计算结果可：
- 自动填充SKU定价表
- 结合国别汇率配置生成多币种售价
- 支持一键应用到批量商品

### 4.4 打通多店铺同步

选品结果可按店铺维度分发：
- 同一商品不同站点差异化定价
- 跨店铺库存同步
- 批量上架到多个授权店铺

---

## 五、工程化实施建议

### 5.1 技术栈推荐

| 层级 | 技术选型 | 理由 |
|------|----------|------|
| 前端框架 | React + TypeScript | 与CBHunter现有架构一致 |
| 状态管理 | Zustand / Redux Toolkit | 策略配置状态持久化 |
| 运算引擎 | 纯TypeScript实现 | 前后端共用，便于测试 |
| 数据源调度 | Node.js Worker线程 | 异步采集，不阻塞UI |
| 数据库 | PostgreSQL JSONB | 存储策略树、配置、映射 |
| 缓存 | Redis | 缓存采集结果，减少重复请求 |

### 5.2 开发优先级

| 阶段 | 内容 | 周期 |
|------|------|------|
| P0 | 原生策略库CRUD + D级策略配置 | 2周 |
| P1 | 基础运算引擎（交集/并集/差集） | 2周 |
| P2 | A级数据源接入（TikTok/Shopee/Temu公域） | 3周 |
| P3 | 复合策略嵌套 + 保存/复用 | 2周 |
| P4 | B级API数据源接入 | 3周 |
| P5 | C级Excel导入 + D级预留激活 | 2周 |
| P6 | UI/UX交互完善（股票选股器范式） | 3周 |

---

## 六、v2 高级动态策略引擎（新增）

### 6.1 引擎升级总览

v1引擎仅支持基础交/并/差/加权运算。v2升级为**量化智能选品引擎**，新增以下能力：

```
┌─────────────────────────────────────────────────────────────────────┐
│                    CBHunter 选品选股器 v2                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │   策略配置层      │  │   智能运算引擎    │  │   结果展示层      │  │
│  │  Strategy Config  │→│   Smart Engine   │→│  Result Display  │  │
│  │                  │  │                  │  │                  │  │
│  │ • 原生策略库(64)  │  │ • 交集/并集/差集 │  │ • 标杆样例商品    │  │
│  │ • 复合策略库      │  │ • 加权评分排序    │  │ • 类目分布       │  │
│  │ • 参数阈值配置    │  │ • 去重/合并       │  │ • 趋势图表       │  │
│  │ • 策略嵌套树      │  │ • 动态权重调配    │  │ • 联动操作入口    │  │
│  │ • 负向筛选规则    │  │ • 负向过滤        │  │ • 实时预览       │  │
│  │ • 策略胜率数据    │  │ • 策略衍生推荐    │  │ • 波动分析       │  │
│  │ • AI辅助生成     │  │ • 回测验证        │  │ • 策略回溯       │  │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘  │
│           │                     │                     │             │
│           └─────────────┬───────┴─────────────────────┘             │
│                         ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │              v2 新增：策略智能服务层                           │    │
│  │         Strategy Intelligence Service Layer                   │    │
│  │                                                             │    │
│  │  • 权重优化建议引擎                                           │    │
│  │  • 相似策略推荐引擎                                           │    │
│  │  • 策略一键衍生引擎                                           │    │
│  │  • 负向规则自动推导                                           │    │
│  │  • 策略胜率统计与归因                                         │    │
│  │  • AI复合策略生成器                                           │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                         ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                    数据源映射层                               │    │
│  │              Data Source Mapping Layer                       │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                         ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │                    数据采集层                                 │    │
│  │                Collection Layer                              │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 6.2 动态权重系统

传统选股器的"权重固定"无法满足跨境选品的动态场景。v2引入**可实时调配的多维权重体系**：

#### 6.2.1 权重维度设计

| 权重维度 | 说明 | 默认值 | 可调范围 |
|----------|------|--------|----------|
| 销量权重(w_sales) | 对销量指标的重视程度 | 0.25 | 0-1 |
| 利润权重(w_profit) | 对毛利率的重视程度 | 0.25 | 0-1 |
| 竞争度权重(w_competition) | 对低竞争度的重视程度 | 0.20 | 0-1 |
| 趋势权重(w_trend) | 对增长趋势的重视程度 | 0.15 | 0-1 |
| 复购权重(w_repurchase) | 对复购潜力的重视程度 | 0.10 | 0-1 |
| 合规权重(w_compliance) | 对合规风险的重视程度 | 0.05 | 0-1 |

#### 6.2.2 权重调节交互

```
用户拖拽滑块调整权重 → 实时重新计算综合得分 → 商品排名即时刷新

示例：
销量权重: ████████░░ 0.30
利润权重: ██████████ 0.35
竞争度权重: ████░░░░░░ 0.15
趋势权重: ██████░░░░ 0.10
复购权重: ███░░░░░░░ 0.07
合规权重: ██░░░░░░░░ 0.03
─────────────────────────
综合得分 = Σ(w_i × score_i)
```

#### 6.2.3 预设权重模板

| 模板名称 | 适用场景 | 销量 | 利润 | 竞争度 | 趋势 | 复购 | 合规 |
|----------|----------|------|------|--------|------|------|------|
| 爆款优先 | 快速找热销品 | 0.40 | 0.10 | 0.15 | 0.20 | 0.10 | 0.05 |
| 利润优先 | 高毛利选品 | 0.15 | 0.40 | 0.10 | 0.10 | 0.15 | 0.10 |
| 蓝海优先 | 低竞争高需求 | 0.15 | 0.15 | 0.40 | 0.15 | 0.10 | 0.05 |
| 稳健经营 | 长期稳定出单 | 0.20 | 0.20 | 0.15 | 0.10 | 0.25 | 0.10 |
| 合规优先 | 规避风险 | 0.15 | 0.15 | 0.10 | 0.10 | 0.10 | 0.40 |
| 自定义 | 用户自由调配 | — | — | — | — | — | — |

### 6.3 智能负向筛选（Negative Filtering）

传统筛选是"正向加条件"，v2引入**反向排除机制**，允许用户先圈定大池再逐步剔除劣质商品：

#### 6.3.1 负向规则类型

| 负向规则ID | 规则名称 | 说明 | 数据来源 |
|-----------|----------|------|----------|
| NF-01 | 排除高退货率 | 自动排除退货率超过阈值的类目/商品 | 卖家后台退货率+公域评论 |
| NF-02 | 排除低利润 | 排除扣除所有成本后利润率低于阈值的商品 | 成本表+售价+运费+佣金 |
| NF-03 | 排除高竞争 | 排除头部卖家集中度超过阈值的类目 | 公域商品列表+卖家分析 |
| NF-04 | 排除违规风险 | 排除触碰禁限售/知识产权红线的商品 | 平台规则+品牌数据库 |
| NF-05 | 排除价格战 | 排除最低价已无利润空间的标准化商品 | 公域比价 |
| NF-06 | 排除衰退品 | 排除处于生命周期衰退期的商品 | 销量趋势+NLP评论分析 |
| NF-07 | 排除低评分 | 排除评分低于阈值的商品 | 公域评论星级 |
| NF-08 | 排除同质化 | 排除搜索结果高度雷同的商品 | 标题相似度+图片相似度 |

#### 6.3.2 正负向组合运算

```
最终商品池 = (正向策略结果 ∩ 负向排除结果) ∪ 白名单商品

示例：
正向：BS-01(榜单Top50) ∩ BL-01(低竞争) ∩ PF-01(毛利率≥30%)
负向：NF-01(退货率<5%) + NF-05(非价格战) + NF-04(无违规风险)
白名单：手动添加的必选商品
结果：同时满足正向条件、不被任何负向规则排除、且在白名单中
```

### 6.4 策略一键衍生（Strategy Derivation）

基于已有策略自动生成变体策略，降低用户构建新策略的门槛：

#### 6.4.1 衍生维度

| 衍生方式 | 说明 | 示例 |
|----------|------|------|
| 参数微调 | 在原策略基础上调整阈值 | BS-01(Top50) → BS-01'(Top30) |
| 平台迁移 | 将策略应用到其他平台 | TikTok榜单 → Shopee榜单 |
| 类目限定 | 增加类目过滤条件 | 全类目爆款 → 包袋类目爆款 |
| 国别限定 | 增加国别过滤条件 | 全站点 → 仅菲律宾站 |
| 周期缩放 | 改变时间窗口 | 7天飙升 → 30天飙升 |
| 组合叠加 | 自动推荐可叠加的策略 | BS-01 → 推荐叠加BL-01+PF-01 |
| 负向增强 | 为现有策略自动添加排除规则 | 爆款筛选 → 排除高退货率 |

#### 6.4.2 衍生流程

```
用户选择策略BS-01
  → 点击「衍生策略」
  → 系统展示7种衍生方向
  → 用户勾选需要的衍生方式
  → 自动生成新策略CS-DERIVE-001
  → 支持继续对该衍生策略做二次衍生
```

### 6.5 相似策略推荐引擎

当用户构建或查看一个策略时，系统自动推荐相似或互补的策略：

```
当前策略：BS-01(榜单Top50) + BL-01(低竞争)

系统推荐：
├── 相似策略：
│   ├── "TikTok菲律宾包袋爆款" (历史命中率高)
│   └── "Shopee马来站3C配件蓝海" (结构类似)
├── 互补策略：
│   ├── PF-01(毛利率筛选) — 补充利润维度
│   ├── CP-02(价格战规避) — 补充竞争维度
│   └── RP-05(稳定日销款) — 补充复购维度
└── 风险预警：
    ├── RC-01(禁限售规避) — 该类目存在合规风险
    └── LC-04(衰退期预警) — 部分商品已进入衰退期
```

### 6.6 策略效果回溯与胜率分析

每次选品运算完成后，系统记录结果并追踪商品后续表现，形成策略胜率数据库：

#### 6.6.1 胜率统计指标

| 指标 | 说明 | 计算方式 |
|------|------|----------|
| 命中率 | 候选商品中实际出单的比例 | 有销量商品数 / 候选商品总数 |
| 平均首月销量 | 候选商品上线首月平均销量 | Σ首月销量 / 候选商品数 |
| 平均毛利率 | 候选商品实际毛利率 | Σ(售价-成本)/售价 |
| 策略ROI | 使用该策略的时间投入产出比 | 商品GMV / 运营时间 |
| 复购率 | 候选商品的30天/60天/90天复购率 | 复购订单数 / 总订单数 |
| 生命周期评分 | 商品存活到当前阶段的比例 | 存活商品数 / 初始候选数 |

#### 6.6.2 胜率分析视图

```
策略 CS-001 历史表现（近90天）
├── 执行次数: 12次
├── 平均命中商品数: 23个
├── 平均首月出单率: 67%
├── 平均毛利率: 34.2%
├── 最佳一次: 45个候选，32个出单
├── 最差一次: 8个候选，2个出单
├── 胜率趋势图: [📈 近30天上升]
└── 归因分析:
    ├── 有利因素: 权重配置合理、数据源稳定
    ├── 不利因素: TR-07数据缺失导致偏差、PF-01成本数据过期
    └── 优化建议: 提高PF-01权重至0.35，补充RP-05策略
```

### 6.7 AI辅助复合策略生成

基于用户的历史偏好和当前市场环境，AI自动生成复合策略建议：

```
输入：用户描述选品目标
  "我想在TikTok菲律宾站找包袋类目的蓝海爆款，要求毛利率30%以上"

AI生成策略树：
{
  "name": "TikTok菲律宾包袋蓝海利润款",
  "operator": "intersection",
  "children": [
    {"ref_id": "BS-01", "config": {"top_n": 50, "country": "PH"}},
    {"ref_id": "BL-01", "config": {"min_search_volume": 500, "max_product_count": 200}},
    {"ref_id": "PF-01", "config": {"target_margin": 0.30}},
    {"ref_id": "RP-05", "config": {"min_daily_sales": 5}},
    {"negative_filters": ["NF-01", "NF-04"]}
  ],
  "weights": {
    "w_sales": 0.25,
    "w_profit": 0.30,
    "w_competition": 0.20,
    "w_trend": 0.10,
    "w_repurchase": 0.10,
    "w_compliance": 0.05
  }
}

输出：
✅ 策略已生成，可直接运行
💡 建议叠加LC-02(成长期加速)观察趋势
⚠️ 注意：PF-01需要导入成本数据才能生效
```

---

## 七、与现有CBHunter系统融合点（v2 扩展）

### 7.1 打通素材图库（v1保留）

选品结果中的标杆商品图片/视频可：
- 一键收藏到CBHunter素材图库
- 作为后续商品发布时的参考素材
- 支持批量下载、分类管理

### 7.2 打通商品发布模块（v1保留）

选品结果可：
- 直接转化为待发布商品草稿
- 复用01_多平台电商后台调研汇总中的字段映射
- 通过02_功能开发方案总文档中的弹窗式编辑流程上架

### 7.3 打通SKU定价模块（v1保留）

利润导向策略（PF系列）的计算结果可：
- 自动填充SKU定价表
- 结合国别汇率配置生成多币种售价
- 支持一键应用到批量商品

### 7.4 打通多店铺同步（v1保留）

选品结果可按店铺维度分发：
- 同一商品不同站点差异化定价
- 跨店铺库存同步
- 批量上架到多个授权店铺

### 7.5 新增：策略市场与共享（v2新增）

- 用户可将自研策略发布到公共策略市场
- 支持按类目、平台、胜率排序浏览他人策略
- 一键订阅他人策略并加入自己的策略库
- 策略贡献者可获得积分/收益分成

---

## 八、工程化实施建议

### 8.1 技术栈推荐（v2 更新）

| 层级 | 技术选型 | 理由 |
|------|----------|------|
| 前端框架 | React + TypeScript | 与CBHunter现有架构一致 |
| 状态管理 | Zustand / Redux Toolkit | 策略配置状态持久化 |
| 运算引擎 | 纯TypeScript实现 | 前后端共用，便于测试 |
| 数据源调度 | Node.js Worker线程 | 异步采集，不阻塞UI |
| 数据库 | PostgreSQL JSONB | 存储策略树、配置、映射 |
| 缓存 | Redis | 缓存采集结果，减少重复请求 |
| AI服务 | LLM API | 策略生成、相似推荐、归因分析 |
| 向量检索 | pgvector / Milvus | 策略相似度检索 |

### 8.2 开发优先级（v2 更新）

| 阶段 | 内容 | 周期 |
|------|------|------|
| P0 | 原生策略库CRUD + D级策略配置 + 64条策略入库 | 2周 |
| P1 | 基础运算引擎（交集/并集/差集/加权） | 2周 |
| P2 | A级数据源接入（TikTok/Shopee/Temu公域） | 3周 |
| P3 | 复合策略嵌套 + 保存/复用 + 版本管理 | 2周 |
| P4 | B级API数据源接入 | 3周 |
| P5 | C级Excel导入 + D级预留激活 + 复购/生命周期策略 | 2周 |
| P6 | UI/UX交互完善（股票选股器范式） | 3周 |
| P7 | 动态权重系统 + 预设模板 | 2周 |
| P8 | 负向筛选规则 + 正负向组合运算 | 2周 |
| P9 | 策略胜率统计 + 回测数据追踪 | 3周 |
| P10 | 策略一键衍生 + 相似策略推荐 | 2周 |
| P11 | AI辅助复合策略生成 | 3周 |
| P12 | 策略市场与共享 | 2周 |

---

**文档版本**: v2.0  
**最后更新**: 2026-07-21
