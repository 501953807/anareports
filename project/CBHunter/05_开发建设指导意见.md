# 开发建设指导意见

> **项目**: CBHunter — 多平台跨境电商卖家后台  
> **版本**: v1.0  
> **日期**: 2026-07-20  
> **核心用户**: 跨境卖家/运营团队  
> **优先级平台**: Shopee（东南亚站点优先）

---

## 一、底层数据库设计

### 1.1 数据库选型建议

| 方案 | 适用场景 | CBHunter推荐 |
|------|----------|-------------|
| PostgreSQL | JSONB支持好、扩展性强、适合复杂查询 | ✅ 推荐 |
| MySQL 8.0 | 生态成熟、团队熟悉度高 | 备选 |
| MongoDB | 文档型、灵活schema | 不推荐（关联查询复杂） |

**推荐**: PostgreSQL 15+，利用JSONB存储平台扩展字段和动态表单数据。

### 1.2 核心表结构

#### 1.2.1 账户与店铺表

```sql
-- 用户账户表
CREATE TABLE users (
    id              BIGSERIAL PRIMARY KEY,
    username        VARCHAR(100) UNIQUE NOT NULL,
    email           VARCHAR(255) UNIQUE NOT NULL,
    password_hash   VARCHAR(255) NOT NULL,
    role            VARCHAR(20) NOT NULL DEFAULT 'seller',
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW()
);

-- 店铺表
CREATE TABLE shops (
    id              BIGSERIAL PRIMARY KEY,
    user_id         BIGINT REFERENCES users(id),
    platform_code   VARCHAR(20) NOT NULL, -- shopee/tiktok_temu/miaoshou
    shop_name       VARCHAR(200) NOT NULL,
    shop_region     VARCHAR(10) NOT NULL, -- PH/SG/MY/TH/VN/ID/UK/US/CN
    shop_type       VARCHAR(20) NOT NULL, -- cross_border/local/full_managed/semi_managed
    status          VARCHAR(20) NOT NULL DEFAULT 'active',
    auth_token      TEXT, -- 平台授权token（加密存储）
    auth_expires_at TIMESTAMPTZ,
    config_json     JSONB DEFAULT '{}', -- 店铺级配置
    created_at      TIMESTAMPTZ DEFAULT NOW(),
    updated_at      TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_shops_user_platform ON shops(user_id, platform_code);
CREATE INDEX idx_shops_region ON shops(shop_region);
```

#### 1.2.2 商品主表

```sql
-- 商品主表（统一商品对象 UPO）
CREATE TABLE products (
    id                  BIGSERIAL PRIMARY KEY,
    shop_id             BIGINT REFERENCES shops(id),
    platform_product_id VARCHAR(100), -- 平台原始商品ID
    title               VARCHAR(500) NOT NULL,
    description         TEXT,
    category_path       VARCHAR(500), -- 类目路径，如 "包袋 > 手提包 > 通勤手提包"
    category_l1         VARCHAR(100),
    category_l2         VARCHAR(100),
    category_l3         VARCHAR(100),
    brand               VARCHAR(100),
    images              JSONB DEFAULT '[]', -- 图片URL数组
    video_url           VARCHAR(500),
    listing_status      VARCHAR(30) NOT NULL DEFAULT 'draft', -- draft/pending_review/listed/offline/violated/deleted
    status              VARCHAR(30) NOT NULL DEFAULT 'active', -- active/inactive
    source_type         VARCHAR(30) NOT NULL DEFAULT 'manual', -- manual/collection/sync
    parent_product_id   BIGINT REFERENCES products(id), -- Temu SPU父级
    variant_group_id    VARCHAR(100), -- Temu SKC组
    supply_price_cny    DECIMAL(10,2), -- Temu供货价
    optimization_tips   TEXT, -- Temu优化事项
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_products_shop ON products(shop_id);
CREATE INDEX idx_products_status ON products(listing_status);
CREATE INDEX idx_products_category ON products(category_l1, category_l2, category_l3);
CREATE INDEX idx_products_parent ON products(parent_product_id);
CREATE INDEX idx_products_variant_group ON products(variant_group_id);
```

#### 1.2.3 SKU表

```sql
-- SKU表
CREATE TABLE skus (
    id                  BIGSERIAL PRIMARY KEY,
    product_id          BIGINT REFERENCES products(id) ON DELETE CASCADE,
    sku_id              VARCHAR(100), -- 平台SKU ID / Model ID
    variant_attributes  JSONB NOT NULL DEFAULT '{}', -- 颜色、尺寸等变体属性
    seller_sku_code     VARCHAR(100), -- 商家货号
    price_cny           DECIMAL(10,2), -- 统一CNY存储
    original_price_cny  DECIMAL(10,2), -- 原价/划线价
    stock_qty           INTEGER NOT NULL DEFAULT 0,
    dimensions          JSONB, -- {length, width, height} cm
    weight_g            INTEGER, -- 重量g
    is_sensitive        BOOLEAN DEFAULT FALSE,
    stock_label_visible BOOLEAN DEFAULT TRUE,
    sku_classification  VARCHAR(50), -- 单品/混合套装
    supply_price_cny    DECIMAL(10,2), -- Temu申报价格
    price_status        VARCHAR(20), -- 开款价格状态
    suggested_retail_price DECIMAL(10,2), -- Temu建议零售价
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_skus_product ON skus(product_id);
CREATE INDEX idx_skus_seller_code ON skus(seller_sku_code);
```

#### 1.2.4 平台扩展字段表

```sql
-- 平台扩展字段（JSONB存储各平台独有字段）
CREATE TABLE product_extensions (
    id                  BIGSERIAL PRIMARY KEY,
    product_id          BIGINT REFERENCES products(id) ON DELETE CASCADE,
    platform_code       VARCHAR(20) NOT NULL,
    ssp_id              VARCHAR(50), -- Shopee标准商品编号
    hot_listing_status  VARCHAR(20), -- Shopee Hot Listing状态
    smart_diagnosis_issues INTEGER DEFAULT 0, -- Shopee Smart Diagnosis问题数
    new_item_boost_eligible BOOLEAN DEFAULT FALSE, -- Shopee新品扶持资格
    advance_stock_available INTEGER DEFAULT 0, -- Shopee Advance Fulfillment可用库存
    advance_stock_in_transit INTEGER DEFAULT 0, -- Shopee在途Advance库存
    sps_score           DECIMAL(3,1), -- TikTok卖家体验分
    crm_enabled         BOOLEAN DEFAULT FALSE, -- TikTok CRM解锁
    chat_plan_quota_total INTEGER, -- TikTok聊天计划总配额
    chat_plan_quota_remaining INTEGER, -- TikTok聊天计划剩余配额
    warehouse_group     VARCHAR(100), -- Temu备货仓组
    stocking_logic      VARCHAR(20), -- Temu JIT/VMI
    daily_posting_quota_used INTEGER DEFAULT 0, -- Temu当日发品已用
    daily_posting_quota_limit INTEGER DEFAULT 400, -- Temu当日发品上限
    qualification_status VARCHAR(20), -- Temu商品资质状态
    live_photo_status    VARCHAR(20), -- Temu实拍图状态
    compliance_info_status VARCHAR(20), -- Temu合规信息状态
    custom_fields        JSONB DEFAULT '{}', -- 其他自定义扩展字段
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);

CREATE UNIQUE INDEX idx_product_extensions_unique ON product_extensions(product_id, platform_code);
```

#### 1.2.5 订单表

```sql
-- 订单主表
CREATE TABLE orders (
    id                  BIGSERIAL PRIMARY KEY,
    shop_id             BIGINT REFERENCES shops(id),
    platform_order_id   VARCHAR(100) NOT NULL, -- 平台原始订单号
    buyer_name          VARCHAR(200),
    order_status        VARCHAR(30) NOT NULL, -- pending_payment/to_process/to_ship/shipped/completed/cancelled/refund_return
    total_amount_cny   DECIMAL(10,2), -- 统一CNY存储
    total_amount_local  DECIMAL(10,2), -- 站点本地币金额
    currency_code       VARCHAR(10) NOT NULL, -- PHP/SGD/MYR/THB/VND/IDR/GBP/USD/CNY
    payment_method      VARCHAR(100),
    shipping_channel    VARCHAR(100),
    tracking_number     VARCHAR(100),
    dispatch_deadline   TIMESTAMPTZ,
    return_refund_id    VARCHAR(100),
    return_package_id   VARCHAR(100),
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);

-- 订单项表
CREATE TABLE order_items (
    id                  BIGSERIAL PRIMARY KEY,
    order_id            BIGINT REFERENCES orders(id) ON DELETE CASCADE,
    product_id          BIGINT REFERENCES products(id),
    sku_id              BIGINT REFERENCES skus(id),
    product_title       VARCHAR(500),
    variant_attributes  JSONB,
    quantity            INTEGER NOT NULL,
    unit_price_cny     DECIMAL(10,2),
    unit_price_local   DECIMAL(10,2),
    total_price_cny    DECIMAL(10,2),
    total_price_local  DECIMAL(10,2)
);

CREATE INDEX idx_orders_shop ON orders(shop_id);
CREATE INDEX idx_orders_status ON orders(order_status);
CREATE INDEX idx_orders_platform_id ON orders(platform_order_id);
```

#### 1.2.6 国别配置表

```sql
-- 国别/站点配置表
CREATE TABLE country_configs (
    id                  BIGSERIAL PRIMARY KEY,
    country_code        VARCHAR(10) NOT NULL UNIQUE, -- PH/SG/MY/TH/VN/ID/UK/US/CN
    currency_code       VARCHAR(10) NOT NULL, -- PHP/SGD/MYR/THB/VND/IDR/GBP/USD/CNY
    currency_symbol     VARCHAR(10) NOT NULL, -- ₱/S$/RM/฿/₫/Rp/£/$/¥
    timezone            VARCHAR(50) NOT NULL, -- Asia/Manila等
    language            VARCHAR(20) NOT NULL DEFAULT 'en',
    tax_rate            DECIMAL(5,4) DEFAULT 0,
    exchange_rate_cny   DECIMAL(10,6) NOT NULL, -- 对CNY汇率
    payment_methods     JSONB DEFAULT '[]', -- 支持的支付方式列表
    logistics_channels  JSONB DEFAULT '[]', -- 物流渠道列表
    category_tree_version VARCHAR(20), -- 类目树版本号
    compliance_requirements JSONB DEFAULT '{}', -- 合规要求
    shipping_fee_template JSONB DEFAULT '{}', -- 运费模板
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);

-- 平台配置表
CREATE TABLE platform_configs (
    id                  BIGSERIAL PRIMARY KEY,
    platform_code       VARCHAR(20) NOT NULL,
    config_key          VARCHAR(100) NOT NULL,
    config_value        JSONB NOT NULL,
    version             INTEGER DEFAULT 1,
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(platform_code, config_key)
);
```

#### 1.2.7 营销活动表

```sql
-- 营销活动表
CREATE TABLE marketing_campaigns (
    id                  BIGSERIAL PRIMARY KEY,
    shop_id             BIGINT REFERENCES shops(id),
    platform_code       VARCHAR(20) NOT NULL,
    campaign_type       VARCHAR(30) NOT NULL, -- discount/flash_sale/voucher/affiliate/chat_plan
    campaign_name       VARCHAR(200) NOT NULL,
    campaign_code       VARCHAR(50), -- 优惠券码等
    status              VARCHAR(20) NOT NULL DEFAULT 'draft', -- draft/active/expired/paused
    start_time          TIMESTAMPTZ NOT NULL,
    end_time            TIMESTAMPTZ NOT NULL,
    target_products     JSONB DEFAULT '[]', -- 关联商品ID数组
    target_categories   JSONB DEFAULT '[]', -- 关联分类ID数组
    discount_rules      JSONB NOT NULL, -- 折扣规则
    total_quantity      INTEGER, -- 可使用总数
    used_quantity       INTEGER DEFAULT 0,
    budget              DECIMAL(10,2), -- 预算
    performance_data    JSONB DEFAULT '{}', -- 销售/订单/买家/CTR统计
    platform_campaign_id VARCHAR(100), -- 平台活动ID
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);

-- 聊天计划表（TikTok CRM）
CREATE TABLE chat_plans (
    id                  BIGSERIAL PRIMARY KEY,
    shop_id             BIGINT REFERENCES shops(id),
    plan_type           VARCHAR(20) NOT NULL, -- one_time/auto
    template_id         VARCHAR(100) NOT NULL, -- 模板ID
    template_name       VARCHAR(200) NOT NULL,
    message_content     TEXT,
    status              VARCHAR(20) NOT NULL DEFAULT 'draft',
    scheduled_at        TIMESTAMPTZ,
    quota_total         INTEGER,
    quota_used          INTEGER DEFAULT 0,
    performance_data    JSONB DEFAULT '{}', -- 聊天GMV/订单数/转化客户数
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);
```

#### 1.2.8 采集箱/发布记录表

```sql
-- 采集箱商品表
CREATE TABLE collection_items (
    id                  BIGSERIAL PRIMARY KEY,
    shop_id             BIGINT REFERENCES shops(id),
    platform_code       VARCHAR(20) NOT NULL,
    source_platform     VARCHAR(50), -- 1688/淘宝/链接等
    source_id           VARCHAR(100), -- 货源ID
    title               VARCHAR(500),
    images              JSONB DEFAULT '[]',
    source_price_cny   DECIMAL(10,2),
    selling_price_cny  DECIMAL(10,2),
    stock_qty           INTEGER DEFAULT 0,
    weight_g            INTEGER,
    pre_category        VARCHAR(500), -- 预发布类目
    group_name          VARCHAR(100), -- 产品分组
    publish_status      VARCHAR(20) NOT NULL DEFAULT 'unpublished', -- unpublished/scheduled/published
    scheduled_at        TIMESTAMPTZ,
    published_at        TIMESTAMPTZ,
    remark              TEXT,
    created_at          TIMESTAMPTZ DEFAULT NOW(),
    updated_at          TIMESTAMPTZ DEFAULT NOW()
);

-- 发布记录表
CREATE TABLE publish_records (
    id                  BIGSERIAL PRIMARY KEY,
    shop_id             BIGINT REFERENCES shops(id),
    product_id          BIGINT REFERENCES products(id),
    platform_code       VARCHAR(20) NOT NULL,
    platform_product_id VARCHAR(100),
    status              VARCHAR(20) NOT NULL, -- success/failed/pending
    error_message       TEXT,
    published_at        TIMESTAMPTZ,
    created_at          TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_collection_items_shop ON collection_items(shop_id, platform_code);
CREATE INDEX idx_collection_items_status ON collection_items(publish_status);
CREATE INDEX idx_publish_records_shop ON publish_records(shop_id, platform_code);
```

---

## 二、接口层与适配层设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      API Gateway                            │
│              (认证、限流、路由、日志)                          │
├─────────────────────────────────────────────────────────────┤
│                    Business Service Layer                    │
│  Products · Orders · Marketing · Finance · Logistics · CRM  │
├─────────────────────────────────────────────────────────────┤
│                  Platform Adapter Interface                  │
│                   (IPlatformAdapter)                        │
├──────────┬──────────┬──────────┬────────────────────────────┤
│Shopee    │TikTok    │Temu      │Miaoshou                    │
│Adapter   │Adapter   │Adapter   │Adapter                     │
└──────────┴──────────┴──────────┴────────────────────────────┘
```

### 2.2 适配器接口定义（更新：支持弹窗式编辑）

```typescript
// 平台适配器基础接口
interface IPlatformAdapter {
    // 平台元信息
    getPlatformCode(): string;
    getPlatformName(): string;

    // 商品操作
    getProductList(params: ProductQueryParams): Promise<ProductPage>;
    getProductDetail(productId: string): Promise<ProductDetail>;
    createProduct(product: UnifiedProduct): Promise<PlatformProductResult>;
    updateProduct(productId: string, product: Partial<UnifiedProduct>): Promise<PlatformProductResult>;
    deleteProduct(productId: string): Promise<boolean>;

    // 类目系统
    getCategoryTree(countryCode: string): Promise<CategoryNode[]>;
    getCategoryFields(categoryId: string, countryCode: string): Promise<FieldSchema[]>;

    // 订单操作
    getOrderList(params: OrderQueryParams): Promise<OrderPage>;
    getOrderDetail(orderId: string): Promise<OrderDetail>;
    shipOrder(orderId: string, trackingNumber: string): Promise<boolean>;

    // 营销操作
    createCampaign(campaign: CampaignConfig): Promise<CampaignResult>;
    getVoucherList(): Promise<Voucher[]>;
    getFlashSaleEligibility(): Promise<FlashSaleEligibility>;

    // 资金操作
    getIncomeSummary(): Promise<FinanceSummary>;
    getTransactionHistory(params: FinanceQueryParams): Promise<TransactionPage>;

    // 权限检查
    checkPermission(module: string): Promise<PermissionResult>;

    // 弹窗编辑支持（新增）
    getEditFormSchema(countryCode: string): Promise<FormSchema>;
    validateEditPayload(payload: any): Promise<ValidationResult>;
}
```

### 2.3 适配器实现要点

#### Shopee Adapter

```typescript
class ShopeeAdapter implements IPlatformAdapter {
    private baseUrl = 'https://api.shopee.cn';
    private region: string; // PH/SG/MY/TH/VN/ID

    async getCategoryTree(countryCode: string): Promise<CategoryNode[]> {
        // 调用Shopee类目API，按站点缓存类目树
        const cacheKey = `shopee:category:${countryCode}`;
        return this.cache.getOrFetch(cacheKey, () =>
            this.api.get('/portal/product/category/tree')
        );
    }

    async getProductList(params: ProductQueryParams): Promise<ProductPage> {
        // 处理Shopee特有的SSP/Hot Listing标签
        const response = await this.api.get('/portal/product/list/all', { params });
        return this.mapToUnifiedProduct(response.data);
    }

    async createProduct(product: UnifiedProduct): Promise<PlatformProductResult> {
        // Shopee四步上架流程：基本信息→销售资料→运费→其他
        // 需要先选择类目，再逐步提交
        const basicInfo = this.extractBasicInfo(product);
        const salesInfo = this.extractSalesInfo(product);
        const shipping = this.extractShipping(product);
        const other = this.extractOther(product);

        return this.api.post('/portal/mtsku/new', {
            basicInfo, salesInfo, shipping, other
        });
    }
}
```

#### TikTok Shop Adapter

```typescript
class TikTokShopAdapter implements IPlatformAdapter {
    private baseUrl = 'https://seller.tiktokshopglobalselling.com';
    private region: string; // PH/SG/MY/TH/VN/ID/UK/US

    async checkPermission(module: string): Promise<PermissionResult> {
        // TikTok跨境账号部分模块无权限
        // 需要检测并返回友好权限结果
        try {
            await this.api.get(`/promotion`);
            return { allowed: true };
        } catch (error) {
            if (error.status === 403) {
                return {
                    allowed: false,
                    reason: '跨境店铺账号缺少此模块权限',
                    requirement: '需要主账号管理员授权'
                };
            }
            throw error;
        }
    }

    async getSPSScore(): Promise<number> {
        // 获取卖家体验分，用于CRM门槛判断
        const response = await this.api.get('/crm/stats');
        return response.data.spsScore;
    }

    async getChatPlanTemplates(): Promise<ChatPlanTemplate[]> {
        // 一次性计划 + 自动计划模板
        const [oneTime, auto] = await Promise.all([
            this.api.get('/crm/templates/one-time'),
            this.api.get('/crm/templates/auto')
        ]);
        return [...oneTime, ...auto];
    }
}
```

#### Temu Adapter

```typescript
class TemuAdapter implements IPlatformAdapter {
    private baseUrl = 'https://agentseller.temu.com';

    async getProductList(params: ProductQueryParams): Promise<ProductPage> {
        // Temu SPU/SKC/SKU三层结构
        const response = await this.api.get('/goods/list', { params });
        return this.mapTemuProduct(response.data);
    }

    mapTemuProduct(data: any): UnifiedProduct {
        return {
            spuId: data.spuId,
            skcId: data.skcId,
            supplyPriceCny: data.supplyPriceCny,
            priceStatus: data.priceStatus,
            suggestedRetailPrice: data.suggestedRetailPrice,
            warehouseGroup: data.warehouseGroup,
            stockingLogic: data.stockingLogic,
            // ...其他字段
        };
    }

    async getComplianceStatus(): Promise<ComplianceStatus> {
        // Temu强制合规项：资质/实拍图/合规信息
        return {
            qualification: await this.checkQualification(),
            livePhotos: await this.checkLivePhotos(),
            complianceInfo: await this.checkComplianceInfo()
        };
    }

    async getInventoryData(spuId: string): Promise<InventoryData> {
        // 仓内库存 + 卖家仓库存 + 销售趋势
        const [warehouse, seller, trend] = await Promise.all([
            this.api.get(`/inventory/warehouse/${spuId}`),
            this.api.get(`/inventory/seller/${spuId}`),
            this.api.get(`/sales/trend/${spuId}`)
        ]);
        return { warehouse, seller, trend };
    }
}
```

### 2.4 数据映射层

```typescript
// 统一商品对象 → 平台特定对象映射
class ProductMapper {
    // UPO → Shopee
    toShopee(product: UnifiedProduct, shop: Shop): ShopeeProduct {
        return {
            title: product.title,
            description: product.description,
            images: product.images,
            categoryPath: product.categoryPath,
            skus: product.skus.map(sku => ({
                sellerSkuCode: sku.sellerSkuCode,
                price: this.convertCurrency(sku.priceCny, shop.currencyCode),
                stock: sku.stockQty,
                attributes: sku.variantAttributes
            })),
            // Shopee特有
            sspId: product.extensions?.sspId,
            hotListingStatus: product.extensions?.hotListingStatus
        };
    }

    // UPO → TikTok Shop
    toTikTok(product: UnifiedProduct, shop: Shop): TikTokProduct {
        return {
            title: product.title,
            description: product.description,
            images: product.images,
            videos: product.videoUrl ? [product.videoUrl] : [],
            categoryPath: product.categoryPath,
            skus: product.skus.map(sku => ({
                sellerSkuCode: sku.sellerSkuCode,
                price: this.convertCurrency(sku.priceCny, shop.currencyCode),
                stock: sku.stockQty
            }))
        };
    }

    // UPO → Temu
    toTemu(product: UnifiedProduct, shop: Shop): TemuProduct {
        return {
            title: product.title,
            description: product.description,
            images: product.images,
            categoryPath: product.categoryPath,
            spuId: product.spuId || this.generateSPUId(),
            skcId: product.skcId || this.generateSKCId(),
            skus: product.skus.map(sku => ({
                skuId: sku.skuId,
                supplyPriceCny: sku.supplyPriceCny || sku.priceCny,
                dimensions: sku.dimensions,
                weightG: sku.weightG,
                stock: sku.stockQty,
                isSensitive: sku.isSensitive
            }))
        };
    }
}
```

---

## 三、动态表单引擎

### 3.1 引擎架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Dynamic Form Engine                       │
├─────────────────────────────────────────────────────────────┤
│  Schema Parser → Field Renderer → Validator → Submitter     │
├─────────────────────────────────────────────────────────────┤
│  Platform Schema Registry                                   │
│  - Shopee: category → fields mapping                        │
│  - TikTok: category → fields mapping                        │
│  - Temu: category → fields mapping                          │
│  - Country Config: locale-specific fields                   │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 表单Schema定义

```json
{
  "formVersion": "1.0",
  "platform": "shopee",
  "country": "PH",
  "steps": [
    {
      "stepId": "basic_info",
      "title": "基本信息",
      "fields": [
        {
          "fieldId": "images",
          "type": "image_upload",
          "label": "商品图片",
          "required": true,
          "maxCount": 9,
          "placeholder": "请上传商品图片"
        },
        {
          "fieldId": "video",
          "type": "video_upload",
          "label": "商品视频",
          "required": false,
          "maxSize": 30,
          "durationMin": 10,
          "durationMax": 60,
          "format": "mp4"
        },
        {
          "fieldId": "title",
          "type": "text_input",
          "label": "商品名称",
          "required": true,
          "maxLength": 180,
          "placeholder": "Input"
        },
        {
          "fieldId": "category",
          "type": "category_selector",
          "label": "类别",
          "required": true,
          "level": 3,
          "dependentFields": ["sales_info", "shipping", "other"]
        },
        {
          "fieldId": "description",
          "type": "textarea",
          "label": "商品描述",
          "required": true,
          "maxLength": 5000
        }
      ]
    },
    {
      "stepId": "sales_info",
      "title": "销售资料",
      "dependsOn": { "fieldId": "category", "notEmpty": true },
      "dynamicFields": true,
      "fields": [
        {
          "fieldId": "skus",
          "type": "dynamic_array",
          "label": "SKU管理",
          "required": true,
          "items": {
            "type": "object",
            "properties": {
              "variantName": { "type": "text_input", "required": true },
              "price": { "type": "number", "required": true, "min": 0 },
              "originalPrice": { "type": "number", "min": 0 },
              "stock": { "type": "number", "required": true, "min": 0 },
              "sellerSkuCode": { "type": "text_input" }
            }
          }
        },
        {
          "fieldId": "brand",
          "type": "select",
          "label": "品牌",
          "required": false
        },
        {
          "fieldId": "attributes",
          "type": "dynamic_category_attributes",
          "label": "类目属性",
          "required": false,
          "dependsOn": { "fieldId": "category" }
        }
      ]
    },
    {
      "stepId": "shipping",
      "title": "运费",
      "dependsOn": { "fieldId": "category", "notEmpty": true },
      "fields": [
        {
          "fieldId": "shippingChannel",
          "type": "select",
          "label": "物流渠道",
          "required": true,
          "dataSource": "platform_config.shipping_channels"
        },
        {
          "fieldId": "weight",
          "type": "number",
          "label": "商品重量(g)",
          "required": true,
          "min": 0
        },
        {
          "fieldId": "dimensions",
          "type": "dimension_input",
          "label": "商品尺寸(cm)",
          "required": true,
          "properties": ["length", "width", "height"]
        }
      ]
    },
    {
      "stepId": "other",
      "title": "其他",
      "dependsOn": { "fieldId": "category", "notEmpty": true },
      "fields": [
        {
          "fieldId": "certifications",
          "type": "dynamic_certifications",
          "label": "认证要求",
          "dependsOn": { "fieldId": "category", "countryCode": "PH" },
          "options": ["NCC", "Anatel"]
        },
        {
          "fieldId": "customFields",
          "type": "dynamic_array",
          "label": "自定义字段",
          "items": {
            "type": "object",
            "properties": {
              "key": { "type": "text_input" },
              "value": { "type": "text_input" }
            }
          }
        }
      ]
    }
  ]
}
```

### 3.3 动态字段渲染逻辑

```typescript
class DynamicFormEngine {
    private schemaRegistry: Map<string, FormSchema>;

    renderForm(schemaId: string, context: FormContext): ReactNode[] {
        const schema = this.schemaRegistry.get(schemaId);
        if (!schema) throw new Error(`Schema not found: ${schemaId}`);

        return schema.steps.map(step => {
            // 检查前置条件
            if (step.dependsOn && !this.evaluateCondition(step.dependsOn, context)) {
                return <Placeholder message="在您选择了商品类目后更新" />;
            }

            // 渲染静态字段
            const staticFields = step.fields
                .filter(f => !f.dynamicFields)
                .map(field => this.renderField(field, context));

            // 渲染动态字段（根据类目加载）
            let dynamicFields = [];
            if (step.dynamicFields && context.categoryId) {
                dynamicFields = this.loadCategoryFields(
                    context.platformCode,
                    context.countryCode,
                    context.categoryId
                );
            }

            return (
                <StepPanel title={step.title}>
                    {[...staticFields, ...dynamicFields]}
                </StepPanel>
            );
        });
    }

    validateForm(schema: FormSchema, formData: FormData): ValidationResult {
        const errors: ValidationError[] = [];

        for (const step of schema.steps) {
            for (const field of step.fields) {
                const value = formData[field.fieldId];

                // 必填校验
                if (field.required && !value) {
                    errors.push({
                        fieldId: field.fieldId,
                        message: `${field.label} 是必填项`
                    });
                    continue;
                }

                // 类型校验
                if (value && !this.validateType(field.type, value)) {
                    errors.push({
                        fieldId: field.fieldId,
                        message: `${field.label} 格式不正确`
                    });
                }

                // 长度校验
                if (field.maxLength && String(value).length > field.maxLength) {
                    errors.push({
                        fieldId: field.fieldId,
                        message: `${field.label} 最多${field.maxLength}个字符`
                    });
                }

                // 数值范围校验
                if (field.min !== undefined && Number(value) < field.min) {
                    errors.push({
                        fieldId: field.fieldId,
                        message: `${field.label} 不能小于${field.min}`
                    });
                }

                // 平台特定校验
                if (field.platformValidation) {
                    const platformError = field.platformValidation(value, formData);
                    if (platformError) {
                        errors.push({
                            fieldId: field.fieldId,
                            message: platformError
                        });
                    }
                }
            }
        }

        return { valid: errors.length === 0, errors };
    }
}
```

### 3.4 类目属性动态加载

```typescript
class CategoryFieldLoader {
    async loadCategoryFields(
        platformCode: string,
        countryCode: string,
        categoryId: string
    ): Promise<FieldSchema[]> {
        // 缓存策略：按平台+国别+类目缓存
        const cacheKey = `${platformCode}:${countryCode}:fields:${categoryId}`;

        return this.cache.getOrFetch(cacheKey, async () => {
            // 从平台适配器获取类目属性
            const adapter = this.adapterFactory.getAdapter(platformCode);
            const fields = await adapter.getCategoryFields(categoryId, countryCode);

            // 注入国别差异化字段
            const countryConfig = await this.countryConfigService.getConfig(countryCode);
            const localeFields = this.getLocaleSpecificFields(platformCode, countryCode);

            return [...fields, ...localeFields];
        });
    }

    private getLocaleSpecificFields(platformCode: string, countryCode: string): FieldSchema[] {
        const fields: FieldSchema[] = [];

        // Shopee菲律宾站：NCC认证
        if (platformCode === 'shopee' && countryCode === 'PH') {
            fields.push({
                fieldId: 'ncc_certification',
                type: 'text_input',
                label: 'NCC认证型号',
                required: false
            });
        }

        // Temu全托管：强制合规字段
        if (platformCode === 'temu') {
            fields.push(
                {
                    fieldId: 'qualification_doc',
                    type: 'file_upload',
                    label: '商品资质文件',
                    required: true
                },
                {
                    fieldId: 'live_photo',
                    type: 'image_upload',
                    label: '商品实拍图',
                    required: true,
                    minCount: 3
                },
                {
                    fieldId: 'compliance_info',
                    type: 'textarea',
                    label: '商品合规信息',
                    required: true
                }
            );
        }

        return fields;
    }
}
```

---

## 四、多币种多费率计算引擎

### 4.1 汇率管理

```typescript
class ExchangeRateService {
    private rateCache: Map<string, { rate: number; updatedAt: Date }>;

    async getExchangeRate(fromCurrency: string, toCurrency: string): Promise<number> {
        if (fromCurrency === toCurrency) return 1;

        const cacheKey = `${fromCurrency}_${toCurrency}`;
        const cached = this.rateCache.get(cacheKey);

        // 5分钟缓存
        if (cached && (Date.now() - cached.updatedAt.getTime()) < 5 * 60 * 1000) {
            return cached.rate;
        }

        // 从汇率服务获取最新汇率
        const rate = await this.financeApi.getExchangeRate(fromCurrency, toCurrency);
        this.rateCache.set(cacheKey, { rate, updatedAt: new Date() });

        return rate;
    }

    async convertAmount(amount: number, fromCurrency: string, toCurrency: string): Promise<number> {
        const rate = await this.getExchangeRate(fromCurrency, toCurrency);
        return Math.round(amount * rate * 100) / 100;
    }
}
```

### 4.2 费用计算引擎

```typescript
interface FeeCalculationResult {
    productAmountLocal: number;
    shippingFee: number;
    platformCommission: number;
    paymentFee: number;
    refundDeduction: number;
    totalFee: number;
    netAmount: number;
    currencyCode: string;
}

class FeeCalculator {
    async calculate(order: Order, shop: Shop): Promise<FeeCalculationResult> {
        const currencyCode = shop.currencyCode;

        // 商品金额
        const productAmount = this.sumOrderItems(order.items);

        // 运费（按物流渠道和国别配置计算）
        const shippingFee = await this.calculateShippingFee(
            order.shippingChannel,
            order.items,
            shop.countryCode
        );

        // 平台佣金（按平台和国别配置）
        const platformCommission = this.calculateCommission(
            productAmount,
            shop.platformCode,
            shop.countryCode
        );

        // 支付手续费（按支付方式）
        const paymentFee = this.calculatePaymentFee(
            productAmount,
            order.paymentMethod,
            shop.countryCode
        );

        // 退款扣除（如有）
        const refundDeduction = order.returnRefundId
            ? await this.getRefundDeduction(order.returnRefundId)
            : 0;

        const totalFee = platformCommission + paymentFee + refundDeduction;
        const netAmount = productAmount - totalFee;

        return {
            productAmountLocal: productAmount,
            shippingFee,
            platformCommission,
            paymentFee,
            refundDeduction,
            totalFee,
            netAmount,
            currencyCode
        };
    }

    private calculateCommission(
        amount: number,
        platformCode: string,
        countryCode: string
    ): number {
        // Shopee佣金率因站点和类目不同
        const commissionRates: Record<string, Record<string, number>> = {
            shopee: {
                PH: 0.05, // 菲律宾5%
                SG: 0.06, // 新加坡6%
                MY: 0.05, // 马来5%
                TH: 0.05, // 泰国5%
            },
            tiktok: {
                PH: 0.04,
                SG: 0.05,
            },
            temu: {
                CN: 0.08 // Temu平台服务费8%
            }
        };

        const platformRate = commissionRates[platformCode]?.[countryCode] ?? 0.05;
        return Math.round(amount * platformRate * 100) / 100;
    }

    private calculatePaymentFee(
        amount: number,
        paymentMethod: string,
        countryCode: string
    ): number {
        // 不同支付方式手续费率不同
        const COD_FEE_RATE = 0.02; // 货到付款手续费2%
        const E_WALLET_FEE_RATE = 0.01; // 电子钱包1%

        if (paymentMethod === 'COD') return Math.round(amount * COD_FEE_RATE * 100) / 100;
        if (['SPayLater', 'MariBank Savings'].includes(paymentMethod)) {
            return Math.round(amount * E_WALLET_FEE_RATE * 100) / 100;
        }

        return 0;
    }
}
```

### 4.3 多币种显示格式化

```typescript
class CurrencyFormatter {
    format(amount: number, currencyCode: string, locale?: string): string {
        const symbols: Record<string, string> = {
            PHP: '₱', SGD: 'S$', MYR: 'RM', THB: '฿',
            VND: '₫', IDR: 'Rp', GBP: '£', USD: '$', CNY: '¥'
        };

        const symbol = symbols[currencyCode] || currencyCode;
        const localeMap: Record<string, string> = {
            PHP: 'fil-PH', SGD: 'en-SG', MYR: 'ms-MY',
            THB: 'th-TH', VND: 'vi-VN', IDR: 'id-ID',
            GBP: 'en-GB', USD: 'en-US', CNY: 'zh-CN'
        };

        return new Intl.NumberFormat(localeMap[currencyCode], {
            style: 'currency',
            currency: currencyCode,
            minimumFractionDigits: 2,
            maximumFractionDigits: 2
        }).format(amount);
    }
}

// 示例输出
// ₱429.00
// S$12.50
// RM 25.00
// ฿150.00
// ₫50,000.00
// Rp 150.000,00
// £25.00
// $12.50
// ¥14.80
```

---

## 五、分类树动态加载方案

### 5.1 类目树数据结构

```typescript
interface CategoryNode {
    id: string;
    name: string;
    nameLocal: string; // 本地化名称
    level: 1 | 2 | 3;
    parentId: string | null;
    path: string[]; // 完整类目路径
    hasChildren: boolean;
    fieldSchemas: FieldSchema[]; // 该类目下的动态字段
    attributes: Record<string, any>; // 类目额外属性
}

interface CategoryTree {
    platformCode: string;
    countryCode: string;
    version: string;
    rootNodes: CategoryNode[];
    updatedAt: Date;
}
```

### 5.2 类目树加载策略

```typescript
class CategoryTreeService {
    private treeCache: Map<string, CategoryTree>;

    async getCategoryTree(
        platformCode: string,
        countryCode: string
    ): Promise<CategoryTree> {
        const cacheKey = `${platformCode}:${countryCode}`;

        // 先查缓存
        const cached = this.treeCache.get(cacheKey);
        if (cached && this.isCacheValid(cached.updatedAt)) {
            return cached;
        }

        // 缓存失效，从平台适配器加载
        const adapter = this.adapterFactory.getAdapter(platformCode);
        const tree = await adapter.getCategoryTree(countryCode);

        // 注入国别差异化字段
        const localeFields = this.getCountrySpecificFields(platformCode, countryCode);
        this.injectFieldsIntoTree(tree, localeFields);

        // 更新缓存
        this.treeCache.set(cacheKey, { ...tree, updatedAt: new Date() });

        return tree;
    }

    async getSubCategories(parentId: string, platformCode: string, countryCode: string): Promise<CategoryNode[]> {
        const tree = await this.getCategoryTree(platformCode, countryCode);
        return this.findChildren(tree.rootNodes, parentId);
    }

    private isCacheValid(updatedAt: Date): boolean {
        // 类目树缓存24小时
        return (Date.now() - updatedAt.getTime()) < 24 * 60 * 60 * 1000;
    }

    private getCountrySpecificFields(
        platformCode: string,
        countryCode: string
    ): FieldSchema[] {
        const fields: FieldSchema[] = [];

        switch (countryCode) {
            case 'PH': // 菲律宾
                if (platformCode === 'shopee') {
                    fields.push({
                        fieldId: 'ncc_certification',
                        type: 'text_input',
                        label: 'NCC认证型号',
                        required: false
                    });
                }
                break;

            case 'TH': // 泰国
                fields.push({
                    fieldId: 'thai_language_title',
                    type: 'text_input',
                    label: '泰语标题',
                    required: false
                });
                break;

            case 'VN': // 越南
                fields.push({
                    fieldId: 'vietnamese_description',
                    type: 'textarea',
                    label: '越南语描述',
                    required: false
                });
                break;

            case 'ID': // 印尼
                fields.push({
                    fieldId: 'halal_certification',
                    type: 'file_upload',
                    label: '清真认证',
                    required: false
                });
                break;
        }

        return fields;
    }
}
```

### 5.3 类目选择器前端组件

```tsx
function CategorySelector({
    platformCode,
    countryCode,
    value,
    onChange,
}: CategorySelectorProps) {
    const [l1Categories, setL1Categories] = useState<CategoryNode[]>([]);
    const [l2Categories, setL2Categories] = useState<CategoryNode[]>([]);
    const [l3Categories, setL3Categories] = useState<CategoryNode[]>([]);
    const [loading, setLoading] = useState(false);

    useEffect(() => {
        loadL1Categories();
    }, []);

    const loadL1Categories = async () => {
        setLoading(true);
        const tree = await categoryTreeService.getCategoryTree(
            platformCode, countryCode
        );
        setL1Categories(tree.rootNodes);
        setLoading(false);
    };

    const handleL1Change = async (l1Id: string) => {
        const children = await categoryTreeService.getSubCategories(
            l1Id, platformCode, countryCode
        );
        setL2Categories(children);
        setL3Categories([]);
        onChange({ l1Id, l2Id: null, l3Id: null });
    };

    const handleL2Change = async (l2Id: string) => {
        const children = await categoryTreeService.getSubCategories(
            l2Id, platformCode, countryCode
        );
        setL3Categories(children);
        onChange({ l1Id: value.l1Id, l2Id, l3Id: null });
    };

    const handleL3Change = async (l3Id: string) => {
        const node = l3Categories.find(c => c.id === l3Id);
        onChange({
            l1Id: value.l1Id,
            l2Id: value.l2Id,
            l3Id,
            categoryPath: node?.path || [],
            categoryFields: node?.fieldSchemas || []
        });
    };

    if (loading) return <Skeleton />;

    return (
        <div className="category-selector">
            <Select
                placeholder="请选择一级类目"
                options={l1Categories.map(c => ({
                    label: c.name,
                    value: c.id
                }))}
                onChange={handleL1Change}
            />

            <Select
                placeholder="请选择二级类目"
                options={l2Categories.map(c => ({
                    label: c.name,
                    value: c.id
                }))}
                disabled={!value.l1Id}
                onChange={handleL2Change}
            />

            <Select
                placeholder="请选择三级类目"
                options={l3Categories.map(c => ({
                    label: c.name,
                    value: c.id
                }))}
                disabled={!value.l2Id}
                onChange={handleL3Change}
            />

            {value.categoryPath && (
                <div className="category-path">
                    当前类目: {value.categoryPath.join(' > ')}
                </div>
            )}
        </div>
    );
}
```

### 5.4 类目树版本管理

```typescript
class CategoryVersionManager {
    async checkForUpdates(
        platformCode: string,
        countryCode: string,
        currentVersion: string
    ): Promise<{ hasUpdate: boolean; newVersion: string }> {
        const latestVersion = await this.platformApi.getCategoryVersion(
            platformCode, countryCode
        );

        return {
            hasUpdate: latestVersion !== currentVersion,
            newVersion: latestVersion
        };
    }

    async invalidateCache(
        platformCode: string,
        countryCode: string
    ): Promise<void> {
        const cacheKey = `${platformCode}:${countryCode}`;
        this.treeCache.delete(cacheKey);
    }
}
```

---

## 六、技术栈建议

### 6.1 前端技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 框架 | React 18 + TypeScript | 组件化开发，类型安全 |
| 路由 | React Router v6 | 多页面SPA |
| 状态管理 | Zustand | 轻量级，适合中大型应用 |
| UI组件库 | Ant Design 5.x | 表格、表单、弹窗等成熟组件 |
| 动态表单 | react-hook-form + zod | 表单验证和渲染 |
| 图表 | ECharts 5.x | 数据分析可视化 |
| 请求库 | Axios | HTTP客户端 |
| 国际化 | react-intl | 多语言支持 |
| 构建工具 | Vite | 快速构建 |

### 6.2 后端技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 运行时 | Node.js 20+ / Go 1.22+ | 高并发I/O |
| 框架 | NestJS / Gin | 模块化架构 |
| ORM | Prisma / GORM | 类型安全数据库操作 |
| 缓存 | Redis | 类目树、汇率、Token缓存 |
| 消息队列 | RabbitMQ / Redis Streams | 异步任务（发布、同步） |
| 定时任务 | Bull / node-cron | 汇率更新、状态同步 |
| 日志 | Winston / Zap | 结构化日志 |

### 6.3 基础设施

| 组件 | 选型 | 说明 |
|------|------|------|
| 数据库 | PostgreSQL 15+ | 主数据库 |
| 缓存 | Redis 7+ | 缓存层 |
| CDN | Cloudflare / 阿里云CDN | 图片/视频加速 |
| 对象存储 | S3 / 阿里云OSS | 商品图片/视频存储 |
| 容器化 | Docker + Kubernetes | 部署编排 |
| CI/CD | GitHub Actions / GitLab CI | 自动化构建部署 |
| 监控 | Prometheus + Grafana | 性能监控 |
| 告警 | AlertManager | 异常告警 |

---

## 七、安全与合规

### 7.1 数据安全

| 措施 | 说明 |
|------|------|
| 敏感数据加密 | 平台授权Token、用户密码使用AES-256加密存储 |
| HTTPS传输 | 所有API通信强制HTTPS |
| 输入校验 | 前后端双重校验，防止SQL注入/XSS |
| 速率限制 | API限流，防止恶意爬取 |
| 审计日志 | 记录所有敏感操作 |

### 7.2 只读优先原则

| 原则 | 实施 |
|------|------|
| 默认只读 | 所有平台适配器默认只读模式 |
| 写操作确认 | 涉及发布、修改、删除的操作必须有二次确认弹窗 |
| 权限隔离 | 子账号权限继承自平台授权，CBHunter不额外赋予写权限 |
| 操作日志 | 所有写操作记录完整日志，可追溯 |
| 资金保护 | 资金相关操作（提现、转账）必须人工确认，禁止自动执行 |

### 7.3 合规要求

| 平台 | 合规要求 |
|------|----------|
| Shopee | 商品资质、实拍图（部分类目）、NCC/Anatel认证（按国别） |
| TikTok Shop | 内容审核、直播规范、广告合规 |
| TEMU | 强制资质上传、实拍图、合规信息、建议零售价合规 |
| 通用 | GDPR（欧盟站点）、各国电商法规、税务合规 |

---

## 八、性能优化

### 8.1 前端性能

| 优化项 | 措施 |
|--------|------|
| 懒加载 | 商品图片懒加载、类目树按需加载 |
| 虚拟滚动 | 长列表使用虚拟滚动（react-virtuoso） |
| 缓存策略 | 类目树、平台配置、汇率数据缓存 |
| 代码分割 | 按平台/模块分割代码，减少首屏加载 |
| PWA | 支持离线访问基础功能 |

### 8.2 后端性能

| 优化项 | 措施 |
|--------|------|
| 数据库索引 | 核心查询字段建立索引 |
| 查询优化 | N+1查询避免、批量查询 |
| 缓存策略 | Redis缓存热点数据 |
| 异步处理 | 耗时操作（发布、同步）异步处理 |
| 分页查询 | 所有列表接口支持分页 |

### 8.3 平台API限流应对

| 策略 | 说明 |
|------|------|
| 请求队列 | 平台API请求排队，避免并发超限 |
| 指数退避 | API限流时指数退避重试 |
| 本地缓存 | 平台数据本地缓存，减少重复请求 |
| 增量同步 | 仅同步变更数据，避免全量拉取 |

---

## 九、开发路线图建议

### Phase 1: 基础框架（4周）

- [ ] 项目脚手架搭建（前端+后端）
- [ ] 数据库表结构创建
- [ ] 认证授权系统
- [ ] 平台适配器接口定义
- [ ] Shopee Adapter MVP

### Phase 2: 核心功能（6周）

- [ ] 商品列表页（Shopee优先）
- [ ] 商品创建/编辑页（动态表单引擎）
- [ ] 订单列表页
- [ ] 营销中心（Shopee折扣/优惠券）
- [ ] 资金概览页

### Phase 3: 多平台扩展（4周）

- [ ] TikTok Shop Adapter
- [ ] Temu Adapter
- [ ] 妙手ERP对接
- [ ] 多站点切换
- [ ] 批量操作功能

### Phase 4: 高级功能（4周）

- [ ] 采集箱功能
- [ ] AI标题优化
- [ ] 数据分析看板
- [ ] 通知中心
- [ ] 移动端适配

### Phase 5: 优化与上线（2周）

- [ ] 性能优化
- [ ] 安全加固
- [ ] 端到端测试
- [ ] 用户验收测试
- [ ] 生产环境部署

**总计**: 约20周
